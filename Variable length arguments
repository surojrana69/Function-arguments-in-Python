#VARIABLE LENGTH ARGEMUMENTS
#Variable-length arguments allow a function to accept any number of arguments

def add_numbers(*args):
  total = 0
  for number in args:
      total += number
  return total

result = add_numbers(55, 50, 55)
print(result)  
